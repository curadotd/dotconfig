#!/bin/bash
# Power menu: hyprshutdown when available (Arch/AUR), rofi power menu elsewhere

if command -v hyprshutdown >/dev/null 2>&1; then
    hyprshutdown --dry-run
else
    "$HOME/.config/rofi/powermenu.sh"
fi
