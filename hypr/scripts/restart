#!/bin/bash
# Restart script - closes Hyprland gracefully then reboots
if command -v hyprshutdown >/dev/null 2>&1; then
    hyprshutdown -t 'Restarting...' --post-cmd 'reboot'
else
    systemctl reboot
fi
